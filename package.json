{
  "name": "risk-based-guardrail",
  "private": true,
  "type": "module",
  "scripts": {
    "build:docs:dist": "node scripts/build-docs-dist.mjs",
    "test": "node --test",
    "test:regen": "node scripts/regen-schedules.mjs"
  },
  "devDependencies": {
    "chart.js": "^4.4.0",
    "esbuild": "^0.25.0"
  }
}